apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: base-masterapp-ingress
  namespace: base-apps
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`dashboard.klocal.ticsmart.eu`)
      services:
        - name: base-mariadb-service
          port: 80
